import 'package:flutter/material.dart';

class FoodItemFormWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          TextFormField(),
          TextFormField(),
          TextFormField(),
        ],
      ),
    );
  }
}
